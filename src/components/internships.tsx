import { CheckCircle2, ChevronRight, FileText, GraduationCap, Users } from "lucide-react";
import type { Student } from "../types";
import { Button, Card } from "./ui";

export function Internships({ students }: { students: Student[] }) { const items = [{ label: "Theory completed", value: students.filter(s=>s.progress>=40).length, icon: GraduationCap }, { label: "Practical completed", value: students.filter(s=>s.progress>=60).length, icon: Users }, { label: "Reports awaiting review", value: students.filter(s=>s.internshipStatus === "Report review").length, icon: FileText }, { label: "Certificates ready", value: students.filter(s=>s.progress===100).length, icon: CheckCircle2 }]; return <div className="page-content"><div className="internship-grid">{items.map(item => <Card key={item.label} className="internship-metric"><item.icon/><b>{item.value}</b><span>{item.label}</span><ChevronRight size={18}/></Card>)}</div><Card><div className="card-head"><div><h2>Milestone tracker</h2><p>Advance learner milestones and keep every internship on track</p></div><Button>Review reports</Button></div>{students.map(s => <div className="milestone-row" key={s.id}><div className="student-cell"><span className="avatar">{s.avatar}</span><span><b>{s.name}</b><small>{s.course}</small></span></div><div className="milestone-steps">{[0,20,40,60,80,100].map((m,i)=><span key={i} className={s.progress >= m ? "done" : ""}><i/>{["Registered","Paid","Theory","Practical","Report","Certificate"][i]}</span>)}</div><b>{s.progress}%</b></div>)}</Card></div>; }
